.container
  .row
     .col-lg-4.col-md-6.col-sm-8.col-xs-12.col-lg-offset-3.col-md-offset-3.col-sm-offset-2
      .panel.panel-default
        .panel-heading
          h3.panel-title Please sign in
        .panel-body
          form(name="loginForm" role='form' novalidate ng-submit="$ctrl.login()")
            fieldset
              .form-group
                input.form-control(placeholder='E-mail', name='email', type='text' ng-model="$ctrl.credentials.email" required)
              .form-group
                input.form-control(placeholder='Password', name='password', type='password', value='',  ng-model="$ctrl.credentials.password" required)
              input.btn.btn-lg.btn-success.btn-block(type='submit', value='Login' ng-disabled="loginForm.$invalid")
  .row(ng-if="$ctrl.loginErrors")
     .col-lg-4.col-md-6.col-sm-8.col-xs-12.col-lg-offset-3.col-md-offset-3.col-sm-offset-2
      .alert.alert-danger.alert-dismissable.fade.in
        a(href="#" class="close" data-dismiss="alert" aria-label="close" ng-click="$ctrl.onClosedAlert()") &times;
        strong Incorrecto
        |  E-mail o password incorrectos.